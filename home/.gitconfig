[core]
	autocrlf = input
	eol = lf
	filemode = false
[color]
	ui = true
[alias]
	amend = commit --amend
	uncommit = reset HEAD^
	undo = reset --soft HEAD^
	show-root = rev-parse --show-toplevel
	show-url = config --local remote.origin.url
