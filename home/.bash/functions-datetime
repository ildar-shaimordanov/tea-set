# shellcheck shell=bash

# =========================================================================

now() {
	date "+%F %T" ${@:+ -d "$@"}
}

today() {
	date "+%F" ${@:+ -d "$@"}
}

tomorrow() {
	today "+1 day $*"
}

yesterday() {
	today "-1 day $*"
}

# =========================================================================

# EOF
